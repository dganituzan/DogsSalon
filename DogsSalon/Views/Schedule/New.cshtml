@model DogsSalon.Models.Order

@{
    ViewData["Title"] = "New";
}


<h1>New Order</h1>

@using (Html.BeginForm("Save", "Schedule"))
{   

    /*@Html.LabelFor(m => m.customer.name)*/
    <div class="form-group">
        @Html.LabelFor(m => m.customerId)
        @Html.TextBoxFor(m => m.customerId, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.customerId)
    </div>

    /*  <div class="form-group">
          @Html.LabelFor(m => m.timeOfOrder, htmlAttributes: new { @class = "control-label col-md-2" })
          <div class="col-md-10">
              @Html.EditorFor(m => m.timeOfOrder, new { htmlAttributes = new { @class = "form-control" } })
              @Html.ValidationMessageFor(m => m.timeOfOrder, "", new { @class = "text-danger" })
          </div>
      </div>
      */

    <div class="form-group">
        @Html.LabelFor(m => m.timeOfOrder)
        @Html.TextBoxFor(m => m.timeOfOrder, "{0: dd/MM/yyyy HH:mm}", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.timeOfOrder)
    </div>

    <div class="form-group">
        @Html.LabelFor(m => m.createDate)
        @Html.TextBoxFor(m => m.createDate, "{0: dd/MM/yyyy}", new { @class = "form-control", @readonly = "readonly" })
    </div>


    /*
        <div class="form-group">
            @Html.LabelFor(m => m.MembershipTypeId)
            @Html.TextBoxFor(m => m.MembershipTypeId,new SelectList(Model. new { @class = "form-control" })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.name)
            @Html.TextBoxFor(m => m.name, new { @class = "form-control" })
        </div>
        */

    @Html.HiddenFor(m => m.id)
    <button type="submit" class="btn btn-primary">Save</button>

    <input type="button" class="btn btn-secondary" value="back" onclick="@("window.location.href='" + @Url.Action("Index", "Schedule") + "'");" />


}






