@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome!</h1>
    <p>Go to </p>
    <input type="button" class="btn btn-outline-primary" value="Schedual" onclick="@("window.location.href='" + @Url.Action("Index", "Schedule") + "'");" />
</div>
